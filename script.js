document.getElementById("bookingForm").addEventListener("submit", function(e) {
  e.preventDefault();
  alert("Your request has been submitted successfully!");
  // TODO: Integrate with backend or Firebase to store requests
});
